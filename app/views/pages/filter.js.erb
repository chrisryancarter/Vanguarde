var $posts = $("<%= escape_javascript(render 'posts') %>");
var container = $('body'),
    scrollTo = $('#titleNews');

$("#newsContainer").empty().append($posts);

container.animate({
    scrollTop: scrollTo.offset().top - container.offset().top + container.scrollTop()
});
