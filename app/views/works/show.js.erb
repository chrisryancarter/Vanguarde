$work = $("<%= escape_javascript(render(@work)) %>");
$("#worksExpand").empty().append($work);
if ($("#worksExpand").is(":hidden")) {
  $("#worksExpand").slideDown('slow');
}
// TODO: move into general js file
$('.close').on('click', function() {
  console.log("clickin all day up in this bitch");
  $('#worksExpand').slideUp();
});
